This exporter collects metrics from Oracle Cloud Infrastructure (OCI) and exposes them in Prometheus format. It supports multiple OCI tenancies using a single OCI user API key and a configuration file.